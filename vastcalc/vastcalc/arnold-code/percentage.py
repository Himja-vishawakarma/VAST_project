# Function to calculate percentage
def percentage(total, percentage):
    return (total * percentage) / 100

# # Taking input from the user
# total_value = float(input("Enter the total value: "))
# percentage_value = float(input("Enter the percentage to calculate: "))

# # Calling the function and printing the result
# result = percentage(total_value, percentage_value)
# print(f"{percentage_value}% of {total_value} is {result}\n")
